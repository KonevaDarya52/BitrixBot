# **Построение карты интернета** <!-- omit in toc -->

**Построение карты интернета** — это мощный инструмент для автоматизированной разведки (OSINT) и визуализации сетевой инфраструктуры.

Проект предназначен для автоматизированного сканирования инфраструктуры домена, визуального построения сетевого графа и инвентаризации активных доменных и IP-ресурсов. На выходе пользователь получает наглядную карту всех связанных доменов, IP-адресов и подсетей.

---

## 📑 Содержание <!-- omit in toc -->

- [ Ключевые возможности](#ключевые-возможности)
- [🛠 Технологический стек](#-технологический-стек)
  - [Backend (Python)](#backend-python)
  - [Frontend (JavaScript/React)](#frontend-javascriptreact)
  - [DevOps \& Infrastructure](#devops--infrastructure)
- [🏛 Архитектура системы](#-архитектура-системы)
  - [Алгоритм работы сканера](#алгоритм-работы-сканера)
- [🚀 Быстрый старт (Установка и запуск)](#-быстрый-старт-установка-и-запуск)
  - [Предварительные требования](#предварительные-требования)
  - [Инструкция по запуску](#инструкция-по-запуску)
  - [🐳 Как пользоваться контейнером docker](#-как-пользоваться-контейнером-docker)
- [Доступ к приложению](#доступ-к-приложению)
- [Использование](#использование)
- [💻 API Документация](#-api-документация)
  - [1. Запуск сканирования](#1-запуск-сканирования)
  - [2. Получение данных графа](#2-получение-данных-графа)
  - [3. Получение статуса (Опционально)](#3-получение-статуса-опционально)
- [Как работает API под капотом?](#как-работает-api-под-капотом)
- [📂 Структура проекта](#-структура-проекта)
- [🔄 История изменений](#-история-изменений)
- [🛡️ Отказ от ответственности](#️-отказ-от-ответственности)

##  Ключевые возможности

- **Глубокое сканирование:** Комбинация DNS-резолвинга, анализа SSL-сертификатов и перебора поддоменов.
- **Интерактивная визуализация:** Построение графа связей на лету с использованием `React Flow` и физического движка `D3-force`.
- **Массовый анализ:** Использование `Masscan` для быстрой проверки целых подсетей (/24).
- **История сканирований:** Сохранение результатов всех сессий в PostgreSQL для последующего анализа.
- **Асинхронная архитектура:** Обработка тяжелых задач в фоне с помощью Celery и Redis.

## 🛠 Технологический стек

Проект построен на современном стеке технологий, обеспечивающем высокую производительность и масштабируемость.

### Backend (Python)

- **Django & Django REST Framework (DRF):** Основной веб-фреймворк и API.
- **Celery:** Асинхронная очередь задач для распределенного сканирования.
- **Redis:** Брокер сообщений для Celery и кэширование.
- **PostgreSQL:** Реляционная база данных для хранения структуры графа.
- **Сетевые инструменты:**
  - `Masscan`: Высокоскоростное сканирование портов.
  - `Cryptography`: Парсинг SSL/TLS сертификатов (x509).
  - `Amass` / `TheHarvester`: OSINT-инструменты для поиска поддоменов.
  - `dnspython`: Работа с DNS-записями.

### Frontend (JavaScript/React)

- **React 18:** Библиотека для построения пользовательского интерфейса.
- **React Flow:** Визуализация интерактивных графов и диаграмм.
- **D3-force:** Физический движок для автоматической укладки (раскладки) узлов графа, предотвращающий наложение объектов.
- **Axios:** Взаимодействие с REST API.

### DevOps & Infrastructure

- **Docker & Docker Compose:** Полная контейнеризация всех сервисов (backend, frontend, db, redis, celery).

## 🏛 Архитектура системы

Система работает по принципу распределенного сбора данных с централизованным управлением через API.

```
graph TD;
    User[Пользователь (React Frontend)] -->|REST API| API[Django Backend];
    API -->|Создает задачу| Redis[(Redis Broker)];
    Redis -->|Забирает задачу| Worker[Celery Worker];

    subgraph Scanner Logic Worker -->|DNS Request| Internet((Internet));
        Worker -->|Masscan /24| Subnet[Подсеть];
        Worker -->|SSL Handshake| Hosts[Хосты];
    end

    Worker -->|Сохраняет связи| DB[(PostgreSQL)];
    API <--|Читает граф| DB;
```

### Алгоритм работы сканера

1. **Инициализация:** Пользователь вводит домен (например, `tyuiu.ru`). Создается сессия сканирования.
2. **DNS-разведка:** Система находит IP-адрес корневого домена.
3. **Инвентаризация подсети:**

   - Определяется подсеть класса C (/24), к которой принадлежит IP.
   - Запускается **Masscan** для проверки 443 порта на всех 254 хостах подсети.

4. **SSL-Harvesting:** Для каждого живого хоста сканер извлекает SSL-сертификат.
5. **Анализ сертификата:** Из полей `Subject Alternative Names (SAN)` и `Common Name (CN)` извлекаются все связанные домены.
6. **Рекурсия:** Найденные новые домены добавляются в очередь на сканирование (в зависимости от заданной глубины).
7. **Визуализация:** Фронтенд получает готовый список узлов и связей, строит граф с помощью силового алгоритма, группируя IP-адреса вокруг подсетей, а поддомены — вокруг родительских доменов.

## 🚀 Быстрый старт (Установка и запуск)

Проект полностью докеризирован. Для запуска вам понадобятся только установленные **Docker** и **Docker Compose**.

### Предварительные требования

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (Windows/Mac) или Docker Engine (Linux) (Примечание: желательно скачать WSL если будете пользоваться докером на винде)
- Git

### Инструкция по запуску

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/Fedorova-Daria/internet_map
cd internet-map
```

2. **Создайте файл переменных окружения (необязательно для локального запуска, но полезно):**

   - В корне проекта уже настроен `docker-compose.yml` с дефолтными настройками для разработки. Ничего менять не нужно.

3. **Запустите проект:**  
   Эта команда соберет образы для бэкенда и фронтенда, скачает базы данных и запустит все контейнеры.

```bash
docker-compose up -d --build
```

**Примечание:** если же выдает ошибку при установке, то лучшим способом будет использование зеркала. Заходим в Settings(шестеренка) -> Docker Engine -> пишем в поле ввода:

```json
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false,
  "features": {
    "buildkit": true
  },
  "registry-mirrors": ["https://mirror.gcr.io"]
}
```

После можно установки можно вернуть прежний вид конфига:

```json
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false
}
```

4. **Дождитесь инициализации:**  
   Первый запуск может занять пару минут (сборка фронтенда и установка зависимостей Python).  
   Проверить статус контейнеров можно командой:

```bash
docker-compose ps
```

5. Создайте админа:
   Для работы с админкой нужно создать суперюзера, где уже можно убирать скан сессии если что-то пошло не так или же посмотреть просканированные домены.

```bash
docker-compose exec web python manage.py createsuperuser
```

### 🐳 Как пользоваться контейнером docker

1. **Отключить/включить контейнер**

   Для этого будет проще не писать команду, а зайти в **docker -> Containers**. Там уже есть контейнер с названием **"internet_map"**, который можно включить/отключить.

2. **Посмотреть логи работы сканнера**

   Посмотреть логи можно 2 способами:
   **1 способ:**
   Заходим **docker -> Containers -> internet_map -> internet_map_celery**
   **2 способ:**
   С помощью команды в терминале:

```bash
docker-compose logs -f celery
```

## Доступ к приложению

После успешного запуска сервисы будут доступны по следующим адресам:

| Сервис          | URL                              | Описание                                  |
| --------------- | -------------------------------- | ----------------------------------------- |
| **Frontend**    | `http://localhost:3000`          | Основной веб-интерфейс (Карта)            |
| **Swagger**     | `http://localhost:8000/swagger/` | Список возможных api ссылок и их описание |
| **Admin Panel** | `http://localhost:8000/admin/`   | Админка Django (просмотр базы)            |
| **Backend API** | `http://localhost:8000/api/`     | REST API Root                             |

## Использование

1. Откройте `http://localhost:3000`.
2. В поле поиска введите целевой домен (например, `tyuiu.ru`).
3. Выберите глубину сканирования (рекомендуется **1-2** для оптимальной скорости).(_Глубина 2 запускает рекурсивный поиск, что может занять более 10 часов. Для быстрого теста используйте глубину 1._).
4. Нажмите **"Сканировать"**.
5. Дождитесь завершения процесса. Граф будет строиться в реальном времени по мере нахождения новых узлов.

**Легенда графа:**

- 🟦 **Синий прямоугольник:** Доменное имя.
- 🟧 **Оранжевый круг:** IP-адрес.
- ⬜ **Серый блок:** Подсеть (/24).
- 🟢 **Зеленая линия:** Прямая DNS/SSL связь.
- 🔴 **Красная линия:** Связь через общий IP.
- 🔵 **Синяя линия:** Иерархия поддоменов.

---

## 💻 API Документация

Бэкенд предоставляет REST API для управления сканированием и получения данных графа.

### 1. Запуск сканирования

Инициирует процесс асинхронного сканирования домена.

- **Endpoint:** `POST /api/domains/scan/`
- **Content-Type:** `application/json`

**Тело запроса:**

```Json
{
"domain": "example.com",
"depth": 2
}
```

- `domain`: Целевой домен (строка, обязательное).
- `depth`: Глубина рекурсивного поиска (целое число, default=2).

**Ответ (Успех - 202 Accepted):**

```json
{
  "status": "started",
  "session_id": 42,
  "domain": "example.com"
}
```

---

### 2. Получение данных графа

Возвращает полный набор узлов и связей для построения визуализации. Поддерживает фильтрацию по домену или ID сессии.

- **Endpoint:** `GET /api/links/graph/`

**Параметры запроса (Query Params):**

- `domain` (обязательный): Имя корневого домена, для которого нужно найти последний скан.
- `session_id` (опциональный): Если нужно получить граф конкретной исторической сессии.

**Пример запроса:**  
`GET /api/links/graph/?domain=example.com`

**Ответ (JSON):**

```json
{
  "domain": "example.com",
  "session_id": 42,
  "nodes": [
    {
      "id": "d-101",
      "label": "example.com",
      "type": "domain",
      "data": "example.com"
    },
    {
      "id": "ip-55",
      "label": "93.184.216.34",
      "type": "ip",
      "data": "93.184.216.34",
      "organization": "EDGECAST"
    }
  ],
  "edges": [
    {
      "id": "e-505",
      "source": "d-101",
      "target": "ip-55",
      "type": "direct",
      "label": "verified_https"
    }
  ],
  "summary": {
    "total_nodes": 2,
    "total_edges": 1
  }
}
```

### 3. Получение статуса (Опционально)

_Внутренняя логика фронтенда использует полинг (опрос) эндпоинта графа. Если граф пустой или сессия в статусе `scanning`, фронтенд ждет._

## Как работает API под капотом?

1. **POST запрос** создает запись `ScanSession` в базе данных и отправляет задачу `internet_map.tasks.scan_domain` в очередь **Redis**.
2. API мгновенно возвращает `session_id`, не дожидаясь окончания сканирования (асинхронность).
3. **Celery Worker** подхватывает задачу, сканирует сеть и наполняет таблицы `Domain`, `IPAddress`, `Link`.
4. **GET запрос** к `/graph/` выполняет сложный SQL-запрос (ORM), который собирает все связи, относящиеся к данной сессии.
5. `views.py` в классе `LinkViewSet` преобразует плоские данные из БД в иерархическую JSON-структуру (Nodes + Edges), вычисляет подсети на лету и отдает фронтенду.

---

## 📂 Структура проекта

```text
internet-map/
├── backend/                 # Django Backend
│   ├── internetmap/         # Конфигурация проекта (settings.py)
│   ├── network/             # Основное приложение
│   │   ├── migrations/      # Миграции БД
│   │   ├── admin.py         # Настройки админки
│   │   ├── models.py        # Модели (Domain, IPAddress, Link, ScanSession)
│   │   ├── scanner.py       # Класс InternetMapScanner (логика сканирования)
│   │   ├── serializers.py   # Сериализаторы для DRF
│   │   ├── tasks.py         # Celery задачи
│   │   ├── tools.py         # Обертки для утилит (Masscan, Amass и др.)
│   │   ├── urls.py          # Маршруты API
│   │   └── views.py         # ViewSets (API endpoints)
│   ├── Dockerfile           # Сборка образа бэкенда
│   ├── manage.py            # Django CLI
│   └── requirements.txt     # Python зависимости
│
├── frontend/                # React Frontend
│   ├── public/
│   ├── src/
│   │   ├── components/      # Компоненты графа
│   │   │   ├── CustomDomainNode.js
│   │   │   ├── CustomIpNode.js
│   │   │   ├── CustomSubnetNode.js
│   │   │   ├── GraphPage.js  # Страница с визуализацией
│   │   │   └── HomePage.js   # Страница запуска скана
│   │   ├── store/           # Управление состоянием (Zustand)
│   │   │   └── store.js
│   │   ├── App.js           # Роутинг
│   │   └── index.js         # Точка входа
│   ├── Dockerfile           # Сборка образа фронтенда
│   ├── package.json         # JS зависимости
│   └── tailwind.config.js   # Стили
│
├── docker-compose.yml       # Оркестрация (Web, DB, Redis, Celery, Frontend)
└── README.md                # Документация
```

## 🔄 История изменений

Полный список изменений доступен в файле [CHANGELOG.md](CHANGELOG.md).

## 🛡️ Отказ от ответственности

Этот инструмент разработан исключительно **в образовательных целях**.

- Авторы не несут ответственности за неправомерное использование данного программного обеспечения.
- Используйте инструмент только для анализа собственных ресурсов или сетей, на тестирование которых у вас есть разрешение.
